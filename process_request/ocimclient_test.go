package process_request

import "testing"

func TestOciManifest(t *testing.T) {
	// oci := OcimageClient{endpoint: "http://localhost:8080"}
	// image, err := oci.Image("ubuntu:10.04")
	// if err != nil {
	// 	t.Fatalf("image failed: %s", err)
	// 	return
	// }
	// t.Logf("ImageID: %s", image.ImageID)
	// manifest, err := image.GetManifest()
	// if err != nil {
	// 	t.Fatalf("manifest failed: %s", err)
	// 	return
	// } else {
	// 	//t.Log(manifest.Layers[0].RequestOptions)
	// 	t.Logf("Result: %s", manifest.Config.Digest)
	// }
}
